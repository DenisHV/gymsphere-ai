import 'package:flutter/material.dart';

// Colores oficiales del design system "High-Performance Kinetic"
class AppColors {
  static const primary = Color(0xFFCCFF00);   // Verde lima (botones, acentos)
  static const secondary = Color(0xFFFFFFFF); // Blanco (texto principal)
  static const tertiary = Color(0xFF2D2E32);  // Gris oscuro (tarjetas)
  static const neutral = Color(0xFF0F1011);   // Negro (fondo)
}